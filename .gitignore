# Исключение кешированных файлов Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Игнорировать директорию виртуальной среды
.venv/

# Игнорировать файл .env с конфиденциальными данными
#.env   # <- если нужно — убери комментарий

# Исключаем файлы зависимостей и кеши IDE
*.swp
*.swo
.vscode/
.idea/
*.log

# Исключаем docker файл (если не нужен в git)
#.docker-compose.yml  # <- если он не нужен — убери комментарий

# Временные файлы и артефакты
celerybeat-schedule.dat
audio.zip
audio/
.pytest_cache/
models/
qwen_model/
